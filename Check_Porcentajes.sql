SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE PROCEDURE CHECK_PORCENTAJES
	@NumCuenta INT
AS
BEGIN
	
	SET NOCOUNT ON;

	DECLARE @TOTAL INT
	
	SELECT @TOTAL = SUM(Porcentaje)
					FROM Beneficiarios
					WHERE NumeroCuenta = @NumCuenta
	
	RETURN @TOTAL
END
GO
